# Tiny ERP OAuth 2.0 Credentials
# Obtenha em: Tiny ERP > Integrações > Aplicativos API V3
TINY_CLIENT_ID=seu-client-id-aqui
TINY_CLIENT_SECRET=seu-client-secret-aqui

# URI de redirecionamento OAuth (opcional, padrão: http://localhost:8080/callback)
TINY_REDIRECT_URI=http://localhost:8080/callback

# Tokens OAuth (para cloud deployment)
# Execute 'npm run auth' localmente e copie o conteúdo de ~/.tiny-mcp/tokens.json
# TINY_TOKENS={"access_token":"...","refresh_token":"...","expires_in":14400,...}

# Configurações do servidor HTTP (apenas para deployment na nuvem)
PORT=3000
HOST=0.0.0.0

# Origens permitidas para CORS (separadas por vírgula, * para todas)
ALLOWED_ORIGINS=*
